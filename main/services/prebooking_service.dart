import '../models/prebooking.dart';

class PrebookingService {
  Future<List<Prebooking>> getPrebookings() async {
    // Simulate fetching prebooking requests from backend.
    return [];
  }

  void createPrebooking(String productId, String userId, String userName, int quantity) {
    // Add functionality to create a prebooking.
  }
}
